<Window x:Class="Screensaver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Screensaver"
        mc:Ignorable="d"
        Height="1080" Width="1920" 
        Title="MainWindow" WindowStyle="None" 
        AllowsTransparency="True" ResizeMode="NoResize"
        Loaded="Window_Loaded" Background="Transparent"
        KeyDown="Window_KeyDown" Closed="Window_Closed" WindowState="Maximized" Cursor="None">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Image x:Name="WallpaperImage"
               Grid.Column="0" Grid.ColumnSpan="2">
            <Image.Effect>
                <BlurEffect Radius="25"/>
            </Image.Effect>
        </Image>

        <TextBlock 
            Text="12:00 AM" Padding="52 0 0 52" 
            VerticalAlignment="Bottom"
            Foreground="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" 
            FontWeight="Bold" FontSize="52"
            Loaded="Clock_Loaded"/>
        <Grid 
            x:Name="Panel" Grid.Column="1" 
            HorizontalAlignment="Stretch" 
            Background="{DynamicResource {x:Static SystemColors.AccentColorBrushKey}}" RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="PanelTransform"/>
            </Grid.RenderTransform>

            <Grid.Effect>
                <DropShadowEffect BlurRadius="75" RenderingBias="Quality" Direction="90"/>
            </Grid.Effect>

            <Grid Margin="32 32 52 52">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Vertical">
                    <TextBlock x:Name="TitleText" Grid.Row="0" Text="Titulo"
                               FontWeight="Bold" FontSize="48"
                               Foreground="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" 
                               TextWrapping="WrapWithOverflow">
                        <TextBlock.RenderTransform>
                            <TranslateTransform x:Name="TitleTransform"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>

                    <Separator Height="12" Background="{x:Null}" Foreground="{x:Null}"/>
                </StackPanel>

                <TextBlock x:Name="BodyText" Grid.Row="1"
                    Text="Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum." 
                    FontWeight="SemiBold" FontSize="28"
                    Foreground="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" 
                    TextWrapping="WrapWithOverflow">
                    <TextBlock.RenderTransform>
                        <TranslateTransform x:Name="BodyTransform"/>
                    </TextBlock.RenderTransform>
                </TextBlock>

                <Image x:Name="HintImage" Grid.Row="2" Source="/Media/bed.png" VerticalAlignment="Center" Height="192">
                    <Image.RenderTransform>
                        <TranslateTransform x:Name="HintImageTransform"/>
                    </Image.RenderTransform>
                </Image>

                <StackPanel Grid.Row="3" Orientation="Horizontal" FlowDirection="RightToLeft">
                    <TextBlock Text="Press any button to close"
                               FontWeight="SemiBold" FontSize="24"
                               Foreground="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
                </StackPanel>
            </Grid>
        </Grid>

        <Grid Grid.Column="0" Grid.ColumnSpan="2" Background="#7F000000"/>
    </Grid>
</Window>
